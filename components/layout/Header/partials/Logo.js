import styled from "@emotion/styled";

const Logo = styled.p`
  color: var(--orange);
  font-size: 4rem;
  line-height: 0;
  font-weight: 700;
  font-family: "Roboto Slab 700", serif;
  margin: 2rem;

  &:hover {
    cursor: pointer;
  }
`;

export default Logo;
